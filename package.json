{
  "name": "sheet-env",
  "version": "1.0.0",
  "description": "Fetch secrets from Google Sheets and inject as environment variables",
  "main": "dist/index.js",
  "bin": {
    "sheet-env": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "dotenv": "^16.4.5",
    "googleapis": "^140.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
